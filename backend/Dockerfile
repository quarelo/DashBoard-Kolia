FROM eclipse-temurin:17-jre

WORKDIR /app

# Placeholder backend container for infra testing when source is missing.
EXPOSE 8080

CMD ["sh", "-c", "echo \"No backend source provided. Container running as placeholder.\" && sleep infinity"]
