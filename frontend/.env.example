# Copie para frontend/.env para desenvolvimento local (fora do Docker).
# No Docker Compose este valor é injetado pelo serviço `frontend`.
# Se ausente, o cliente usa http://localhost:8080 como padrão.
VITE_API_URL=http://localhost:8080
